/* ═══════════════════════════════════════════════════════════════════════
   TEMPLATE 06 · ASCOFIZZ ORIGINAL — THE BASELINE STOREFRONT
   ───────────────────────────────────────────────────────────────────────
   This is the existing Ascofizz website, unchanged. No new navigation, no
   new page structure, no new components, no new product layout, no change
   to cart, checkout, admin or business logic — the point of template 06 is
   that the storefront a customer already runs keeps working, untouched, on
   the same platform the other five templates are built on.

   The only thing this pack adds is the demo switcher, so the baseline can
   be reached from the other five storefronts and compared against them.
   No data-tpl attribute is set, so not one rule of the brand-template
   layer applies to this page.
   ═══════════════════════════════════════════════════════════════════════ */
(function () {
  WL.baseline({
    slug: 'ascofizz-original',
    name: 'Ascofizz Original',
    tagline: 'Existing storefront · baseline template'
  });
})();
