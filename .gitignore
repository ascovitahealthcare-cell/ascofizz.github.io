node_modules/
.env
.env.local
.env.production
*.log

# Wrangler writes local state here. It sits inside the assets directory (the
# repo root), so it must stay out of git AND out of the deploy — see .assetsignore.
.wrangler/

# Standalone storefront bundles — regenerate with
#   python3 scripts/build-storefronts.py --bundle
dist/
